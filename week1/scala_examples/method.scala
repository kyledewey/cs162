// a function that takes no argumemts and doesn't do anything
def noArgs() {}

// a function that takes one argument and doesn't do anything
def oneArg(x: Int) {}

// a function that takes one argument and returns it
def oneArgReturnsIt(x: Int): Int = x

// a private function that takes no arguments and doesn't do anything
private def privateFunc() {}
